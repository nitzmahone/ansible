---
- hosts: localhost
  gather_facts: False
  vars:
    item_A: yes
  tasks:
  - assert:
      that: item is defined
      quiet: True
#    loop_control:
#      loop_var: item
    loop:
    - item_A
#    with_items:
#      - item_A
  - assert:
      that: item is defined
      quiet: False
#    loop_control:
#      loop_var: item
    loop:
    - item_A
