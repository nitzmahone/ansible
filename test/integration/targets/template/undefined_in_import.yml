- hosts: localhost
  gather_facts: false
  tasks:
    - raw: echo {{ lookup('template', 'undefined_in_import.j2') }}
      ignore_errors: true
      register: res

    - assert:
        that:
          - res.failed
          # FIXME: update this once we know what the error message will be
          - res.msg is contains("undefined BLAH FIXME")
          # - "\"'undefined_variable' is undefined\" in res.msg"
